$schema: https://json.schemastore.org/lefthook.json
pre-commit:
  commands:
    test:
      run: pnpm test
    type-check:
      run: pnpm type-check & pnpm --filter docs type-check
    check:
      run: pnpm check
      stage_fixed: true
